// Replace this with your actual Render URL when you want to connect to production,
// OR replace with your computer's local IP address (e.g. http://192.168.1.5:5001) for local testing on a physical device.
// 10.0.2.2 is the special alias for your host loopback interface (localhost) on the Android Emulator.
export const API_URL = 'http://10.0.2.2:5001';
